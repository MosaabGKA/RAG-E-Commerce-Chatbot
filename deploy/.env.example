# Groq API key for the RAG generation step (gpt-oss-20b / gpt-oss-120b)
GROQ_API_KEY=

# Optional: override the Groq model used for generation
GROQ_MODEL=openai/gpt-oss-20b

# Set to 1 to include the raw prompt messages in the /chat API response (debugging)
RAG_DEBUG=0
